#include <stdio.h>
#include <stdlib.h>

int main()
{
    float x,y,z;

    printf("x=");
    scanf("%f",&x);
    printf("y=");
    scanf("%f",&y);

    z=x*y-(x*x*x-y*y*y)/(x*x+2*x*y+y*y);

    printf("z=%.3f",z);
    return 0;
    }
